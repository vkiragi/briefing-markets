[theme]
# Dark theme with indigo accents to match the chart palette.
# Flip to light anytime via top-right ⋮ menu → Settings → Theme.
base = "dark"
primaryColor = "#6366f1"
backgroundColor = "#0f1116"
secondaryBackgroundColor = "#1a1d28"
textColor = "#e5e7eb"
font = "sans serif"
